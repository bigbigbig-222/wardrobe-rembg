services:
  - type: web
    name: wardrobe-rembg-api
    env: python3
    plan: standard
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn rembg_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
